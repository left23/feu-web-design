{% extends '../_layout.twig' %}

{% block body %}
<body class="{{ three.class }}">
<div class="background-image background-image-{{ three.class }}"></div>
{% endblock %}

  {% block hero %}

    <div class="hero {{ three.class }}">

      <div class="hero__bgimage-overlay"></div>

      <div class="hero__shape">

        <div class="container">
          <h1>{{ three.title }}</h1>
          <h3>{{ three.subtitle }}</h3>

        </div>

      </div>

    </div>

  {% endblock %}